/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        label 'node12'
    }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
